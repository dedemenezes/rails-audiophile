<div class="container">
  <div class="row py-4">
    <% @orders.each do |order| %>
      <div class="col-md-3"><%= cl_image_tag order.product.get_main_image(session[:device]).key, width: '100%' %></div>
      <div class="col">
        <h1>Checkout summary</h1>
        <p>Purchase of <strong><%= order.product.name %></strong> for <strong><%= humanized_money_with_symbol order.amount %></strong></p>
        <button id="pay" class="btn btn-primary">Pay</button>
        <!-- # Commented for the lecture, remove comments.
        <script src="https://js.stripe.com/v3/"></script>
        <script>
          const paymentButton = document.getElementById('pay');
          paymentButton.addEventListener('click', () => {
            const stripe = Stripe('<%= ENV['STRIPE_PUBLISHABLE_KEY'] %>');
            stripe.redirectToCheckout({
              sessionId: '<%= order.checkout_session_id %>'
            });
          });
        </script> -->
      </div>
    <% end %>
  </div>
</div>
